{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs and opening http(s)/mailto links in the system browser or mail app.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
